@model IEnumerable<DelVaksinClient.ServiceSemuaData.Vaksin>

@{
    ViewBag.Title = "Vaksin Digunakan";
}

<style type="text/css">
    th {
        text-align: center;
    }
</style>

<div class="jumbotron" style="background-color: seagreen">
    <h2 style="color: white; margin-top: -20px; margin-bottom: -15px;">Daftar vaksin yang telah digunakan</h2>
</div>

<div style="background-color: whitesmoke">
    <div style="padding: 20px" class="table-responsive">
        <table id="example" class="display" style="width:100%">
            <thead style="background-color: darkgrey">
                <tr style="color: white">
                    <th>
                        ID Vaksin
                    </th>
                    <th>
                        Nama Vaksin
                    </th>
                    <th>
                        Aksi
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    if (item.status == 8)
                    {
                        <tr>
                            <th>
                                @Html.DisplayFor(modelItem => item.ID_vaksin)
                            </th>
                            <th>
                                @Html.DisplayFor(modelItem => item.nama)
                            </th>
                            <th>
                                <button class="btn btn-info" onclick="location.href='@Url.Action("Detail", new { id = item.ID_vaksin })';return false;">Detail <i class="glyphicon glyphicon-eye-open"></i></button>
                            </th>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
<br />
@section scripts{
    <script src="~/Content/js/jquery-3.5.1.js"></script>
    <script src="~/Content/js/jquery.dataTables.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#example').DataTable();
        });
    </script>
}
<link href="~/Content/css/jquery.dataTables.min.css" rel="stylesheet" />